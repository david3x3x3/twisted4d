<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:glEsVersion="0x00030000" android:required="true" />
    <uses-feature android:name="android.hardware.gamepad" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Twisted4D">

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:screenOrientation="unspecified"
            android:theme="@style/Theme.Twisted4D">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- screenOrientation was "landscape" (hard-locked) until the portrait virtual-controller
             work; see HypercubeRenderer.onSurfaceChanged's doc for how portrait's square-pinned-
             to-the-top layout is computed. "unspecified" (not "sensor"/"fullSensor") deliberately
             still respects the device's own rotation-lock setting, same as most apps: this adds
             portrait as a possible orientation, it doesn't force rotation independent of what
             the user already has their device set to. -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="unspecified"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout" />

        <!-- Lets MainActivity.shareLogFile share a real, properly-named/extensioned file (e.g.
             "twisted4d.log") via a content:// Uri, instead of loose clipboard text that every
             receiving app has to invent a filename/extension for. See that function's doc. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
