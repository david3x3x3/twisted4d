[package]
name = "puzzle-core"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "puzzle_core"
crate-type = ["cdylib"]

[dependencies]
jni = "0.21"
rand = "0.8"

[profile.release]
lto = true
